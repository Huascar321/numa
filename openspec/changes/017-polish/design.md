# Design

Use exact Home/Transactions/Plan/Analytics/More navigation and exact More
items. Apply mobile-first responsive black/graphite/gray/white direction,
reduced motion, safe-area/env(), dvh/VisualViewport, install education, stale
network-first finance reads, limited idempotent outbox writes, explicit HTTPS
geolocation, no background location, and no iOS background-sync dependency.
