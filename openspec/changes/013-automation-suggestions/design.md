# Design

Store rule/AI suggestion source, evidence, schema/prompt versions, scope,
review decision, and acceptance actor/time. Accepted changes remain bounded to
allowed metadata/category automation and never create final transactions.
QR glosa suggestions are bounded to category/subtype review: raw glosa is
immutable, missing glosa is not synthesized, and the common observed default is
not treated as guaranteed.
