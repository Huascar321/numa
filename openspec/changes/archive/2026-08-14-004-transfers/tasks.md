# Tasks

## 1. PostgreSQL migration and immutable persistence

- [x] 1.1 Add Alembic revision `004_transfers` after `003_ledger_core`; verify a migration from an empty database, a migration from `003_ledger_core`, and a second `upgrade head` no-op leave the expected schema and revision unchanged.
- [x] 1.2 Create `transfers` and `transfer_legs` with `NUMERIC(38,18)` originals, positive derived `NUMERIC(76,38)` rate, common `TIMESTAMPTZ`, no float columns, required creation fingerprints, Plan-scoped composite foreign keys, distinct-account and anti-self-reversal checks, unique root/role/leg identities, unique direct-reversal linkage, and indexes for Plan activity and reversal lookup.
- [x] 1.3 Add the UTF8 PostgreSQL canonical text function and `BEFORE INSERT` Transfer trigger; implement exactly NFC then U+0020-only edge trimming, preservation of internal/non-U+0020 whitespace and case, rejection of U+0000--U+001F/U+007F/U+0080--U+009F, code-point limits, memo empty-to-NULL, reversal-reason parent/nullability, and cross-required/same-NULL rate source. Add constraints that enforce the same persisted canonical states and direct-SQL tests that compare them with API canonicalization/fingerprints.
- [x] 1.4 Evolve Transaction and posted-movement checks so `transfer` is valid only with no Category while income/expense still require Categories; retain the redundant movement classifier only with SQL-enforced equality to Transaction type, preserve one original movement per transfer leg, and reject correction/compensation/replacement state for every `Transaction(type=transfer)`.
- [x] 1.5 Add the deferred PostgreSQL pair-integrity mechanism and the exact quotient/remainder half-even rate helper; prove it permits root, Transactions, legs, and movements within one transaction but rejects at commit a root missing a leg, duplicate/missing roles, wrong Plan/Account/currency/amount/time, equal Accounts, non-original/missing movement, wrong movement sign, non-positive/wrong `NUMERIC(76,38)` rate, wrong rate-source currency state, orphan transfer Transaction, orphan transfer movement, income/expense Transaction linked as a leg, and every Transaction/movement classifier mismatch.
- [x] 1.6 Add SQL append-only guards for Transfer roots, legs, transfer Transactions, and their movements; prove direct UPDATE, DELETE, detachment, and insertion of a generic correction for any `Transaction(type=transfer)` are rejected without changing history.
- [x] 1.7 Update `api/app/db.py` to `EXPECTED_REVISION = "004_transfers"`; test `/health/ready` returns `200` at `004_transfers` and `503` at `003_ledger_core`, and assert Alembic head equals the runtime expected revision.

## 2. Domain posting, reversal, and idempotency

- [x] 2.1 Define typed create/read/reversal contracts with client UUIDs, strict decimal strings, offset-aware timestamps, and one Python/SQL-equivalent canonicalization contract for memo, reversal reason, and rate source; canonicalize before fingerprints and include all three named persisted values (including nulls), provenance, leg role, grouped activity, and exact money/rate serialization.
- [x] 2.2 Implement same-currency posting in one PostgreSQL transaction; prove it creates exactly one root, outbound/inbound Transactions, links, and original movements, equal absolute amounts, 38-place rate one, absent/NULL rate source, and correct negative/positive balances.
- [x] 2.3 Implement cross-currency posting in one PostgreSQL transaction with Python `Decimal` local `prec = 160`, never float, and 38-place `ROUND_HALF_EVEN` quantization; prove both Account-scale originals, shared timestamp, non-empty canonical source, `outbound / inbound` orientation, lower and upper half-even ties, maximum ratio, minimum ratio, both orientations, and the proof-backed `NUMERIC(76,38)` bounds without conversion.
- [x] 2.4 Reject before commit cross-Plan ownership, identical/unknown/archived Accounts, account-currency mismatch, zero or nonrepresentable amounts, floats, missing/invalid canonical timestamp/text fields, a present same-currency rate source, same-currency inequality, non-positive/out-of-range derived rate, out-of-domain amount overflow `1e20 / 1e-18`, out-of-domain amount underflow `1e-19 / M`, and sign/role violations; assert no root, leg, movement, or idempotency identity survives each failure.
- [x] 2.5 Implement client-UUID creation idempotency with canonical fingerprints; prove sequential identical replay returns `201` then `200`, NFC/U+0020-equivalent replay has the same fingerprint, payload conflict returns `409`, and no duplicate root or legs are written.
- [x] 2.6 Implement synchronized concurrent creation tests for identical and conflicting UUID payloads; assert respectively one `201` plus one `200`, and one `201` plus one `409`, with exactly one durable pair and no `500` or partial effects.
- [x] 2.7 Implement idempotent reversal by locking the source root and atomically creating a linked compensatory root/pair; prove prior history is unchanged, root-only canonical memo is retained, ordinary roots have null reversal reason, compensating roots have a non-empty canonical reversal reason, direct-SQL self/non-compensatory reversal data is rejected, swapped Accounts/currencies/originals/signs are exact, cross-currency `rate_source = "reversal"` and same-currency source is NULL, rate is derived from swapped originals rather than a rounded inverse, the maximum-ratio reversal stores the minimum orientation, and both Account balances net as expected.
- [x] 2.8 Implement sequential and concurrent reversal replay/conflict handling; prove identical requests yield `201`/`200`, different reversal UUIDs against one source yield `201`/`409`, the unique reversal link leaves exactly one durable compensatory pair, and a reversal may be reversed only as an exact child of its immediate parent.

## 3. Plan-scoped HTTP API and canonical activity

- [x] 3.1 Add strict schemas and Plan-scoped `PUT /plans/{plan_id}/transfers/{transfer_id}`, `GET /plans/{plan_id}/transfers`, `GET /plans/{plan_id}/transfers/{transfer_id}`, and `PUT /plans/{plan_id}/transfers/{transfer_id}/reversals/{reversal_id}` routes; expose canonical root memo/reversal reason and rate source only for cross currency, reject invalid canonical forms and a present same-currency source, and assert observable `201`, `200`, `404`, `409`, and `422` responses match the contracts.
- [x] 3.2 Extend read contracts so a transfer leg is visibly identified by `transfer_id` and role while Plan Transaction/activity listing presents exactly one grouped Transfer item containing both legs and root-owned memo/reversal reason; prove neither leg is represented as unrelated income or expense or as authority for root text.
- [x] 3.3 Confirm no Transfer PATCH/DELETE/leg-unlink route exists and the generic Transaction correction route returns `409` immediately for any `Transaction(type=transfer)`, including a corrupt/unlinked one, before reading/building correction history or snapshots and with no correction, movement, or projection change.
- [x] 3.4 Add Plan-isolation API tests for root, list, leg activity, and reversal access; assert cross-Plan requests return `404` and disclose or mutate no data.

## 4. Derived projections and reporting boundaries

- [x] 4.1 Extend exact Account balance projection tests for same- and cross-currency transfers and reversals; assert both Accounts sum their immutable movements at their own declared scales with no authoritative stored balance.
- [x] 4.2 Update the monthly summary/envelope selectors and tests so transfer movements contribute to none of income, expense, category activity, Ready to Assign, Assigned, Available, budget activity, or `unconverted_by_currency`, including cross-currency transfers and reversals; prove `_unconverted_summary` classifies expense only by explicit `transaction_type = 'expense'`, never by a non-income fallback, and SQL rejects classifier mismatches before projection.
- [x] 4.3 Preserve the durable `transfer` classifier and add the analytics-boundary contract test that a grouped Transfer is excluded from analytics income/expense/net totals while normal unconverted handling remains explicit; do not introduce analytics aggregation or conversion in this change.

## 5. Minimal authoritative web client

- [x] 5.1 Implement a Plan-scoped Transfer form that permits only distinct active same-Plan Accounts, validates exact Account-scale originals and common timestamp, requires canonical rate source only for cross currency, omits it for same currency, displays the same-currency equality rule, and previews cross-currency `outbound / inbound` evidence without calculating a local balance.
- [x] 5.2 Implement grouped Transfer activity/detail presentation that shows both legs, original amounts/currencies, shared timestamp, rate, rate source when cross-currency, root memo, root reversal reason, and reversal relationship; verify it is not labelled income, expense, Category activity, or budget activity.
- [x] 5.3 Implement a reversal action with a fresh client UUID, required canonical timestamp/reason, conflict/replay feedback, and no edit, generic correction, unlink, or delete controls for roots or legs.
- [x] 5.4 After successful create or reversal, invalidate and refetch authoritative Plan Transactions/activity, both Accounts, and active Budget queries; test that the client performs no local balance, budget, or conversion delta.

## 6. Verification and scope guard

- [x] 6.1 Run empty/`003_ledger_core`/repeated-head migration and readiness-revision suites, PostgreSQL canonicalization/direct-SQL, bidirectional constraint-trigger, SQL-immutability, rollback, create/reversal idempotency, synchronized concurrency, Plan isolation, 38-place FX maximum/minimum/both-orientation/tie/max-reversal and out-of-domain amount suites, balance, budget-exclusion, and analytics-boundary suites; retain assertions for exactly one root and two legs per durable Transfer, no orphan transfer Transaction/movement, no classifier mismatch, no equal-account/self/non-compensatory reversal, and early correction rejection.
- [x] 6.2 Run focused web tests for validation, grouped rendering, FX evidence, reversal, forbidden controls, authoritative refetch, and total budget/activity exclusion; run the production web build.
- [x] 6.3 Run `openspec validate 004-transfers --strict --no-interactive`, `openspec validate --all --strict --no-interactive`, and `git diff --check`; mark a task complete only after its implementation and stated observable verification pass.
- [x] 6.4 Confirm this change adds no exchange execution, Quote persistence/lookup, silent currency conversion, reconciliation workflow, destructive financial mutation, transfer editing, generic correction of any `Transaction(type=transfer)`, or analytics aggregation implementation.
